# Void Wyrm runtime archive

The Void Wyrm boss runtime is not included in the current release. Its server plugin sources are preserved under `server/` with the non-compiling `.java.disabled` extension and must remain outside the auto-compiled plugin tree until the boss is release-ready.

Art assets, packed sprite entries, the appearance reservation, importer, and tests remain available for future development.

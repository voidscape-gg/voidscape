# Prelaunch readiness

- 2026-06-27: Portal slice fixed `/privacy` and `/data-deletion`, added launch-open countdown handoff, and made public launch-signup account nav usable on desktop/mobile.
- Verified this slice with portal API/schema smokes, hosted launch verifier rehearsal, browser future/past countdown checks, signup -> account manager browser flow, mobile-width browser nav/overflow checks, package rehearsal, and `scripts/build.sh`.
- 2026-06-27: Added `scripts/smoke-portal-prelaunch-visual.sh` so prelaunch/account QA is screenshot-backed instead of only API-backed. Local isolated launch-signup visual proof passed at `tmp/prelaunch-portal-visual-v3/smoke`: desktop landing, real account + first OpenRSC character creation against a copied game DB, account-ready success, dashboard, character roster, subscription card waiting state, security panel, mobile landing, mobile dashboard, and mobile nav drawer. Isolated launch-open takeover proof passed at `tmp/prelaunch-portal-open-visual-v2/smoke`: desktop/mobile `Voidscape is open`, zero countdown, launch proof visible, downloads/client chooser visible, and no horizontal page overflow.
- 2026-06-27: Live non-mutating portal visual proof passed at `tmp/prelaunch-portal-live-visual-v1/smoke` against `https://voidscape.gg/`: desktop and mobile landing screenshots are current, countdown is active, signup form is reachable, and both desktop/mobile page overflow checks passed.
- 2026-06-27: Provider placeholder audit remains plug-and-play: Google ID-token signup is enabled only by `PORTAL_GOOGLE_CLIENT_ID` and can override JWKS with `PORTAL_GOOGLE_JWKS_URL`; SMTP, Turnstile, and payments are reserved in the staging env template but inert until handlers are implemented. Verification after the visual script: `scripts/test-portal-api.sh`, `scripts/test-portal-schema.sh`, local `scripts/verify-launch-staging.mjs --skip-server-config --skip-web-verify --run-signup --allow-http`, `node --check web/portal/dev-server.mjs`, `node --check web/portal/api-smoke.mjs`, `bash -n scripts/smoke-portal-prelaunch-visual.sh scripts/package-launch-staging.sh`, and targeted `git diff --check` all passed.
- Live portal was deployed and smoked on `https://voidscape.gg/`: `/privacy`, `/data-deletion`, `/transparency`, `/api/health`, `/api/public`, browser account manager, and launch-signup account flow passed. Backup: `/opt/voidscape/backups/portal-prelaunch-20260627T041258Z.tgz`.
- 2026-06-27: Live `/play/` was visually checked and redeployed for mobile existing-login ergonomics. The Android/web-mobile login panel now has larger field spacing/tap height; marker QA proved a lower username tap still edits username and password taps edit password. Live backup: `/opt/voidscape/backups/play-prelaunch-mobile-login-fields-pre-20260627T044345Z.tgz`.
- Current live `/play/` package: client version `10120`, generated `2026-06-27T04:43:06Z`, `fileCount: 421`, asset token `cache-356134644e1c926b5ee2`, manifest SHA-256 `ed5258a9582536093b29f86b12089c0ec8edeab4dc89e2c898e37e0b0b5a0212`, JS SHA-256 `8eedd1d267f79eb5ef45ed18dadc085580cebdb347eb79658b8676aa61d05502`.
- Hosted `/play` verification passed with deep manifest and smoke at `tmp/web-teavm-deployment-verify-prelaunch-mobile-login-fields-smoke-v1`: 421/421 manifest files verified, cache policy clean, WSS `wss://voidscape.gg/play/ws/`, account/recovery portal handoffs opened, login succeeded, post-resume input proved, zero failed requests, zero unexpected console. The fresh QA account starts in the Void Council intro at `(24,37)`, so the smoke accepts queued movement input without requiring actual terrain movement until the intro is completed.
- Builds passed after this slice: `scripts/build-web-teavm-spike.sh`, `scripts/package-web-teavm.sh --skip-build --output-dir dist/web-teavm`, `scripts/build.sh`, `scripts/build-android.sh`, and `bash -n scripts/smoke-web-teavm-iphone.sh`.
- 2026-06-27: Entitlement/name reservation hardening now blocks code-only founder reservations from promising names already held by another founder, another portal roster character, or an existing OpenRSC player row. Existing game names may refresh only with the same canonical player email. Regression coverage added for game-only and portal-character squatting attempts; `scripts/test-portal-api.sh`, `scripts/test-portal-schema.sh`, `node --check web/portal/dev-server.mjs`, `node --check web/portal/api-smoke.mjs`, and `git diff --check` passed. Deployed to live portal after backup `/opt/voidscape/backups/portal-pre-entitlement-hardening-20260627T050844Z.tgz`; live health/public/privacy/data-deletion passed and a squatter probe against existing game name `Qa89a047b4` returned `409 username_reserved`.
- Launch staging template now keeps `PORTAL_STARTER_IP_DAILY_LIMIT=10` aligned with `PORTAL_SIGNUP_IP_DAILY_LIMIT=10`, matching the current live prelaunch promise that accepted accounts get the starter card unless public copy/policy changes.
- 2026-06-27: Android APK visual login/character proof now passes locally against a launch-signup-created OpenRSC save. Temporary launch portal on `127.0.0.1:8891` created `Droid08d127` in `server/inc/sqlite/preservation.db` with `source: openrsc-sqlite-created`, `linkStatus: linked`, User group `10`, `web_account_id=1`, and `starter_card:1=1`; backup before this local DB mutation is `tmp/prelaunch-client-qa/preservation-before-android-qa-20260626T221455.db`. Android smoke artifact `tmp/prelaunch-client-qa/android-auth-login-v4` proves APK install, local endpoint `10.0.2.2:43596`, visible login fields, `login response:64`, and first-login post-login invite/subscription panel. Harness fix: authenticated Android smokes now write the requested endpoint by default, use calibrated portrait login taps, and tap visible `Ok` if hardware Enter does not submit. Earlier attempts found stale app-private live endpoint state and a `voidscape_api35` System UI/Google-service ANR; disabling `com.google.android.as` in the disposable AVD and rerunning fixed the emulator noise.
- 2026-06-27: Android returning-player lifecycle visual proof now passes at `tmp/prelaunch-client-qa/android-auth-lifecycle-v15`. The run installs the rebuilt APK, logs into `Droid08d127`, captures in-game HUD, backgrounds/resumes, duplicate-launches through the wrapper, opens the Game settings tab, logs out through the visible `Click here to logout` row, returns to the branded login home, and proves the Existing User keyboard still opens. Root fix: the Android smoke log/script had been using the Social-tab logout Y while the Game tab drew its row at `logoutY=334`; shared-client Game-tab hover/click/log math now agrees, and the passing log shows `ANDROID_SMOKE_SETTINGS_LOGOUT tab=game hit=true clickSignal=true`. Follow-up visual QA caught the classic Android `Basic options` heading still colliding with `Camera angle`; the redundant classic Android Game/Android headings are now skipped, and v15 proves the first settings row is readable.
- 2026-06-27: Local desktop web client now also passes against the same portal-created save. Visual QA exposed a server-side local WebSocket bug: with no SSL cert configured, `RSCMultiPortDecoder` constructed `OptionalSslHandler(null)`, so `ws://127.0.0.1:43496/` closed before handshake. Fix: only add `OptionalSslHandler` when `server.getSSLContext()` is non-null. `scripts/build.sh` passed after the fix, server restarted, and `scripts/smoke-web-teavm-desktop.sh --no-build --static-dir dist/web-teavm --host 127.0.0.1 --ws-port 43496 --user Droid08d127 --pass <local QA password> --out tmp/prelaunch-client-qa/web-desktop-portal-created-v2` passed with `login response:64`, resource-control proof, keyboard proof, no failed requests/recent errors, and `movement.onboardingIntro: true` because fresh characters start at `(24,37)`.
- 2026-06-27: Desktop Java client now has a screenshot-backed prelaunch gate. Added `scripts/smoke-pc-client-prelaunch.sh` and login/host/port/output arguments to `scripts/run-workbench-client.sh`. Login-screen visual proof passed at `tmp/prelaunch-client-qa/pc-client-login-screen-visual-v2`; authenticated fresh-cache proof passed at `tmp/prelaunch-client-qa/pc-client-auth-visual-v3` against `Droid08d127`, capturing in-game HUD, inventory, stats, minimap, and account drawer. Visual QA caught local ignored `Client_Base/Cache/accounts.txt`/`credentials.txt` residue showing `test / Combat: 123`; the smoke now temporarily backs those files up by default, proves the fresh-user account drawer (`Droid08d127 / Combat: 3`), then restores the local cache.
- 2026-06-27: Desktop launcher now has a screenshot-backed prelaunch gate. Added smoke-only launcher capture properties and `scripts/smoke-launcher-prelaunch.sh`. Live-style launcher visual proof passed at `tmp/prelaunch-client-qa/launcher-live-visual-v1` with `https://voidscape.gg` and `voidscape.gg:43596`; launch staging package rehearsal passed at `tmp/prelaunch-client-qa/launch-staging-package-v2` with sidecar and embedded launcher properties verified; packaged-jar visual proof passed at `tmp/prelaunch-client-qa/launcher-staging-jar-visual-v2` using `--use-packaged-config`. The packager now also removes the temporary source-side launcher properties file after staging builds when none existed before.
- 2026-06-27: Android release packaging path is now plug-and-play but guarded. `scripts/build-android.sh --release` requires upload signing config, `scripts/package-launch-staging.sh --android-release` copies release APKs and records `android_apk_type=release`, and debug package rehearsals record `android_apk_type=debug`. Android-enabled debug staging package passed at `tmp/prelaunch-client-qa/launch-staging-package-v3-debug-android`; unsigned release guard passed at `tmp/prelaunch-client-qa/android-release-signing-guard-v1`.
- 2026-06-27: Android smoke SDK discovery is now less brittle for QA machines: it checks `ANDROID_HOME`, `ANDROID_SDK_ROOT`, `Android_Client/local.properties`, common Android Studio/Homebrew SDK roots, `adb` on `PATH`, and explicit `ANDROID_SMOKE_ADB` / `ANDROID_SMOKE_EMULATOR` overrides.
- 2026-06-27: Live `/play/` iPhone Simulator Safari visual orientation pass completed at `tmp/prelaunch-client-qa/iphone-simulator-live-play-v1`. Portrait, raw landscape, and upright landscape screenshots passed the script's screenshot/color checks; visual inspection showed the login card readable and Create Account/Existing User reachable, with Safari helper controls not covering the form. This is Mobile Safari Simulator proof only, not physical iPhone/Home Screen proof.
- 2026-06-27: Added `docs/PRELAUNCH-QA-HANDOFF.md` as the compact-ready evidence map and rerun guide. Final hygiene for the visual QA slice passed: shell syntax for changed scripts, Node syntax for portal/package scripts, targeted `git diff --check`, launcher source-resource cleanup (`PC_Launcher/src/main/resources/voidscape-launcher.properties` absent), and canonical `scripts/build.sh`.
- 2026-06-27: Added `scripts/check-prelaunch-readiness.sh` as the one-command non-device report runner. It ties portal schema/API, canonical build (unless skipped), launch-staging package rehearsal, packaged desktop launcher visual smoke, optional Android release guard, optional live portal visual smoke, and optional authenticated desktop Java smoke into `summary.md` plus per-check logs. Validation run `tmp/prelaunch-readiness-nondevice-v1/summary.md` passed with portal schema, portal API/entitlements, staging package, bundle manifest, and packaged launcher visual checks green; build was skipped in that run because `scripts/build.sh` had already passed in this slice.
- 2026-06-27: Launch-staging bundles now generate `RELEASE-HANDOFF.md` with target URLs, predeploy private-config checks, backup path blanks, suggested backup commands, deploy steps, rollback steps, and AGPL source-disclosure confirmation. `VERIFY-STAGING.sh` is now bundle-relative for the packaged web manifest/server config and accepts `VOIDSCAPE_REPO_ROOT` so it remains usable after syncing the bundle to a host. `scripts/check-prelaunch-readiness.sh` now fails if `RELEASE-HANDOFF.md` is missing; validation run `tmp/prelaunch-readiness-handoff-v1/summary.md` passed with 6 passes and 0 failures, including the generated handoff.
- 2026-06-27: Superseded: an earlier pass gated public Android APK downloads behind explicit env. That policy was later rejected by the owner and reversed; current source should show the APK in the post-launch chooser when the APK artifact exists.
- 2026-06-27: Superseded: the earlier Android APK gate was deployed to live and proved unavailable there. Current source has now restored the APK download behavior, but that restore still needs live sync if production should show it.
- 2026-06-27: Cleaned up public AGPL source disclosure. Static landing, privacy, and data-deletion pages now link `https://github.com/voidscape-gg/voidscape` instead of upstream-only OpenRSC, matching `/api/integrity` source metadata. `scripts/test-portal-api.sh` now asserts public-mode pages contain the Voidscape mirror and not the upstream-only link. Deployed to live after backup `/opt/voidscape/backups/portal-pre-source-link-fix-20260627T090827Z.tgz`; live `/`, `/privacy`, `/data-deletion`, and `/api/integrity` source-link checks passed; live visual smoke passed at `tmp/prelaunch-portal-live-source-link-v1`.
- 2026-06-27: Added and deployed the public config-health gate. `/api/health.config` now exposes only booleans/issue codes for launch readiness, requiring a non-placeholder `PORTAL_ABUSE_HASH_SALT` in public mode and reporting admin-token readiness without leaking values. Live deploy backup: `/opt/voidscape/backups/portal-pre-config-health-20260627T091509Z.tgz`. Live `/api/health` reports `publicReady:true`, `abuseHashSaltConfigured:true`, `adminTokenConfigured:true`, and `issues:[]`; hosted non-mutating verifier passed at `tmp/launch-staging-live-nonmutating-config-health-v1/summary.json` with `/play` deep manifest 421/421 and cache policy 0 failures. Local validation passed with `scripts/test-portal-api.sh`, Node syntax checks, shell syntax checks, and targeted `git diff --check`.
- 2026-06-27: Added `scripts/build-prelaunch-visual-board.mjs` so game/client QC can start from one screenshot-backed HTML board instead of opening scattered artifact folders. Generated `tmp/prelaunch-visual-board-game-v1/index.html`; `summary.json` reports 7 sections, 37 screenshots, and 0 missing across portal/account, countdown takeover, hosted desktop/mobile web, iPhone Simulator, desktop launcher, desktop Java client, and Android APK evidence. Serve the repo root locally to view it in a browser, e.g. `python3 -m http.server 8898 --bind 127.0.0.1 --directory .` then open `/tmp/prelaunch-visual-board-game-v1/index.html`.
- 2026-06-27: Added `--visual-board` to `scripts/check-prelaunch-readiness.sh` and refreshed the aggregate non-device readiness report at `tmp/prelaunch-readiness-visual-board-v1/summary.md`: 7 pass, 0 fail, 4 expected warnings for skipped canonical build, Android release-signing guard, live portal visual smoke, and authenticated desktop Java smoke. The generated board for this report is `tmp/prelaunch-readiness-visual-board-v1/visual-board/index.html`; the launch-staging handoff is `tmp/prelaunch-readiness-visual-board-v1/launch-staging-package/RELEASE-HANDOFF.md`.
- 2026-06-27: Closed the hosted server-config verifier gap without copying secrets. Extracted only public-safe launch config lines from live `/opt/voidscape/server/local.conf` into `tmp/live-server-config-verification-v1/server-public-launch-values.conf`, then ran `scripts/verify-launch-staging.mjs` against live portal/web/WSS with `--server-config` and `--skip-signup --no-web-smoke`. It passed at `tmp/launch-staging-live-server-config-v1/summary.json`: server branding `Voidscape`, client version `10120`, custom-version enforcement true, packet registration false, pcap false, production command lockdown true, member world true, websockets true, portal config ready, web deep manifest 421/421, and cache policy 0 failures. Only warnings were intentional non-mutating choices: staged signup skipped and web smoke skipped.
- 2026-06-27: Refreshed the aggregate readiness report with live visual proof enabled: `scripts/check-prelaunch-readiness.sh --out tmp/prelaunch-readiness-live-visual-board-v1 --skip-build --visual-board --live-portal-visual` passed with 8 pass, 0 fail, and 3 expected warnings for skipped canonical build, Android release-signing guard, and authenticated desktop Java smoke. This run includes portal schema/API, launch-staging package, generated handoff, packaged launcher visual smoke, live portal visual smoke, and visual evidence board.
- 2026-06-27: Ran the full automatable non-device aggregate: `scripts/check-prelaunch-readiness.sh --out tmp/prelaunch-readiness-full-nondevice-v1 --visual-board --live-portal-visual --android-release-guard` passed with 10 pass, 0 fail, and 1 warning for the credentialed desktop Java smoke. This run includes canonical `scripts/build.sh`, portal schema/API, launch-staging package, generated handoff, packaged launcher visual smoke, Android release-signing guard, live portal visual smoke, and visual evidence board. Android release guard passed by proving unsigned release APKs are blocked until upload signing is configured. Also fixed the aggregate runner so future `--pc-user` desktop Java smokes receive the target `--host` and `--port` instead of defaulting to local settings.
- 2026-06-27: Created disposable live QA account `Qa094648` through the public launch-signup verifier; `tmp/launch-staging-live-signup-websmoke-v1/summary.json` passed with real account creation, one linked OpenRSC save, starter card waiting, login/readback, server config proof, hosted `/play` smoke, web deep manifest 421/421, and cache policy 0 failures. Patched `scripts/verify-launch-staging.mjs` to redact token/password-like fields before writing response artifacts or command logs, then scrubbed the existing signup verifier artifact; credential checks confirmed the password and unredacted tokens are absent from the checked artifact directories.
- 2026-06-27: Ran live-host desktop Java smoke with the disposable QA account. `tmp/prelaunch-client-qa/pc-client-live-credential-v1/manifest.json` passed against `voidscape.gg:43596`, capturing in-game, inventory, stats, map, and account panels for `Qa094648` at `(24,37)`. The full aggregate `scripts/check-prelaunch-readiness.sh --out tmp/prelaunch-readiness-full-live-credential-v1 --visual-board --live-portal-visual --android-release-guard --pc-user Qa094648 --pc-pass-file <local secret>` then passed with 11 pass, 0 warn, 0 fail; its regenerated visual board now reports 7 sections, 44 screenshots, and 0 missing.
- 2026-06-27: Filled the generated release handoff for the current live prelaunch state at `tmp/prelaunch-readiness-full-live-credential-v1/launch-staging-package/RELEASE-HANDOFF.md` with real live backup paths, all-green evidence links, source-disclosure proof, and explicit remaining manual gates. Created compact archive `tmp/prelaunch-release-handoff-current-v1.tgz` containing the filled handoff, bundle `MANIFEST.txt`, sanitized verifier summaries, visual-board summary plus desktop/mobile overview PNGs, live desktop Java manifest, and public-safe server config proof. Leak checks passed against the handoff directory and signup/web-smoke artifacts.
- 2026-06-27: Tightened the prelaunch evidence loop for visual QC and low-disk safety. `scripts/check-prelaunch-readiness.sh` now refuses to start by default if the output filesystem has less than 512 MiB free, because bundles/screenshots can fail halfway on this machine; use `--min-free-mb` only for intentional smoke tests or clean superseded `tmp/prelaunch-*` evidence first. The handoff/checklist now point QC to the visual board as the fastest game/client review surface. The board now includes global QC focus notes, per-section review focus, and manual-gate callouts before the screenshot grid; latest regenerated board has 7 sections, 44 screenshots, and 0 missing. Added `scripts/run-android-device-qa.sh` and `scripts/validate-android-device-qa-report.py` so direct APK QA can require a filled real-device report. Quick visual review caught Android portrait settings heading crowding, so the redundant classic Android Game/Android settings headings are now skipped; the tab strip already labels those panels.
- 2026-06-27: Added stricter visual/link-click coverage for the portal landing so visible CTAs must point at real visible sections, account nav targets must exist, policy routes must return OK, and the AGPL source link must point at the Voidscape mirror. Visual testing caught two dead footer paths: live still had `data-landing-scroll="features"` with no matching section, and the local artboard footer exposed a Whitepaper button whose section is intentionally zero-height. Local fixes changed the footer Features target to `gameplay`, removed the hidden Whitepaper footer button, and tightened `scripts/smoke-portal-prelaunch-visual.sh`; local non-mutating proof passed at `tmp/prelaunch-portal-local-link-clicks-v1` with 21 checks and screenshots. Regenerated the visual board at `tmp/prelaunch-readiness-full-live-credential-v1/visual-board/index.html`; it reports 7 sections, 44 screenshots, 0 missing, and now uses the live CTA click-map tile. A pre-sync live probe at `tmp/prelaunch-portal-live-link-clicks-v1` failed as expected on stale live footer wiring, then the post-sync live proof below closed that gap.
- 2026-06-27: Deployed the portal footer/CTA fix to live after backup `/opt/voidscape/backups/portal-pre-footer-cta-fix-20260627T110009Z.tgz`, copying only `web/portal/index.html` because the live/local diff was exactly the footer `Features` target plus hidden `Whitepaper` removal. Live non-mutating link-click smoke passed at `tmp/prelaunch-portal-live-link-clicks-v2` with 22 checks and screenshots, proving visible Reserve/Features/footer Home targets, policy routes, AGPL source link, and mobile landing wiring on `https://voidscape.gg/`. Non-mutating hosted launch verifier also passed at `tmp/launch-staging-live-nonmutating-footer-cta-fix-v1` with expected warnings for skipped signup and web smoke; it rechecked live portal config health, Android unavailable-by-default, server config proof, `/play` deep manifest 421/421, and cache policy 0 failures. The visual board now uses the live CTA click-map tile rather than local-only proof.
- 2026-06-27: Expanded the portal visual smoke to cover the signed-in account manager, not only signup. The smoke now clicks dashboard quick actions for Characters, Subscription, Security, and Features, then creates a second game login through the Character Manager UI via `/api/characters`. Isolated local proof passed at `tmp/prelaunch-portal-account-manager-v1/smoke` with 48 checks: first launch-signup character, dashboard quick actions, second character API `201`, 2-character roster screenshot, dashboard `2 / 10`, starter card still waiting, security panel, and signed-in mobile nav. The visual board now points portal/account tiles at this stronger proof and reports 7 sections, 44 screenshots, 0 missing.
- 2026-06-27: Refreshed the portal/account visual proof to include a visible fallback for players who lose recovery codes. `web/portal/index.html` Security tab now says to email `support@voidscape.gg` from the account email with character names for staff review; `scripts/smoke-portal-prelaunch-visual.sh` asserts the note and `mailto:` link. Isolated visual proof passed at `tmp/prelaunch-portal-account-manager-recovery-fallback-v1/smoke` with 50 checks and 11 screenshots. The main visual board now points at this refreshed account-manager proof, and board-only gate proof passed at `tmp/prelaunch-readiness-recovery-fallback-board-v1` with 7 sections, 44 screenshots, 0 missing, and 2 overview PNGs.
- 2026-06-27: Deployed the recovery-support fallback copy to live by syncing only `web/portal/index.html` after backup `/opt/voidscape/backups/portal-pre-recovery-fallback-20260627T122024Z.tgz`; remote SHA-256 matched local `d2d1ab7d8c650abd4a7c0eed54f815e859288b3330399b9881944500b30c4518`. HTTPS now serves `recovery-support-note` plus `mailto:support@voidscape.gg`. Live non-mutating portal visual smoke passed at `tmp/prelaunch-portal-live-recovery-fallback-v1` with 22 checks and 0 failures; hosted non-mutating verifier passed at `tmp/launch-staging-live-nonmutating-recovery-fallback-v1` with 41 checks, 0 failures, and only expected skipped-signup/web-smoke warnings.
- 2026-06-27: Strengthened `scripts/test-portal-api.sh` with a launch-signup second-character entitlement regression. The fixture now creates `LaunchAlt` through authenticated `/api/characters`, proves it is a second linked OpenRSC save, proves the roster grows to 2, and asserts the visible starter-card entitlement plus the fixture DB `starter_card:*` marker count stay unchanged, so the free card remains account-level and is not minted per character. Rerun passed.
- 2026-06-27: Extended the same launch-signup portal API regression to cover public-mode account recovery. The fixture now generates eight one-time recovery codes for `LaunchGuy`, rejects an invalid recovery code, uses one valid code to reset the web password, proves exactly one code is consumed, proves a fresh session is issued, proves the original launch signup/login sessions are revoked, proves the old password no longer works, and proves the new recovered password logs in. Focused readiness proof passed at `tmp/prelaunch-readiness-portal-recovery-gate-v1`, whose report line says the Portal API smoke covers account signup/login, character creation, starter-card invariants, launch-mode recovery reset, and session revocation.
- 2026-06-27: Retuned the visual board wording for game/client QC. Hosted web mobile portrait/landscape tiles now explicitly call out the `Design Your Character` flow, and the desktop web tile calls out a portal-created first-login subscription/invite dialog over the live game canvas. Main board regenerated at `tmp/prelaunch-readiness-full-live-credential-v1/visual-board` with 7 sections, 44 screenshots, 0 missing; archive evidence summary synced. Playwright render check passed on 1440x1200 and 390x900 with no horizontal overflow and the character-design callout present.
- 2026-06-27: Refreshed Android lifecycle visuals after fixing the classic Android settings heading collision. `scripts/build-android.sh --debug` passed, then `scripts/android-smoke.sh --no-build --only-auth-lifecycle` passed at `tmp/prelaunch-client-qa/android-auth-lifecycle-v15`, proving login, HUD, resume, duplicate relaunch, readable Game settings tab, logout, and post-logout keyboard. The visual board now uses v15 for Android lifecycle tiles; board-only readiness check passed at `tmp/prelaunch-readiness-android-v15-qc-smoke`, and Playwright render check passed on 1440x1200 and 390x900 with Android v15 present and no stale v13 warning.
- 2026-06-27: Made the visual board itself visual-first. `scripts/build-prelaunch-visual-board.mjs` now loads gallery images eagerly and renders `board-review.png` plus `board-review-mobile.png` after waiting for every tile image, preventing black offscreen placeholders in long-page overview screenshots. `scripts/check-prelaunch-readiness.sh --visual-board` now requires those two overview PNGs before reporting the board green. Regenerated `tmp/prelaunch-readiness-full-live-credential-v1/visual-board`; summary reports 7 sections, 44 screenshots, 0 missing, and overview PNGs present. Board-only gate proof passed at `tmp/prelaunch-readiness-board-overview-gated-v1` with `7 sections, 44 screenshots, 0 missing, 2 overview PNGs`. Visual inspection confirmed the hosted web, desktop launcher/client, and Android sections now show real thumbnails in the overview images.
- 2026-06-27: Added a login-only, non-mutating live account-manager visual path to `scripts/smoke-portal-prelaunch-visual.sh` with `--login-email` plus `--login-password-file`. Live proof passed at `tmp/prelaunch-portal-live-account-manager-login-v1`: 40 checks, 9 screenshots, signup summary null, no new account/character/card/recovery-code creation, and dashboard/characters/subscription/security/mobile nav all visible for disposable QA account `Qa094648`. The main visual board now includes these live account-manager tiles; regenerated `tmp/prelaunch-readiness-full-live-credential-v1/visual-board` and focused board gate `tmp/prelaunch-readiness-live-account-board-v1` both report 7 sections, 47 screenshots, 0 missing, and 2 overview PNGs.
- 2026-06-27: Integrated physical-device evidence hooks into `scripts/check-prelaunch-readiness.sh`. The aggregate runner now accepts `--android-device-report` to run `scripts/validate-android-device-qa-report.py`, plus `--iphone-qa-report --iphone-local-preflight --iphone-package-dir` to run `scripts/check-web-teavm-iphone-final-release.py`; missing reports remain warnings. Focused no-heavy-work proof passed at `tmp/prelaunch-readiness-device-report-hooks-v1`, showing the new Android/iPhone report rows and manual-gate instructions. Final device signoff should rerun the full aggregate with these report paths after real Android and iPhone QA are filled.
- 2026-06-27: Superseded: an earlier Android public-channel decision withheld direct APK links. Owner later asked to restore the APK to the post-launch chooser; current source follows that correction and still recommends physical Android QA before broad promotion.
- 2026-06-27: Generated the live physical iPhone QA packet at `tmp/iphone-web-qa-live-prelaunch-v1/iphone-safari-qa-report.md` using `scripts/run-web-teavm-iphone-qa.sh --base-url https://voidscape.gg/play/ --ws wss://voidscape.gg/play/ws/ --portal https://voidscape.gg/ --deployment-summary tmp/launch-staging-live-signup-websmoke-v1/web-deployment/summary.json`. The report is prefilled with live `/play`, WSS, portal URLs, and hosted deployment verifier JSON (`smokeRan/smokePassed`, deep manifest 421/421, cache policy 0 failures); physical iPhone diagnostics/checklist blocks remain for the tester.
- 2026-06-27: Corrected the Android APK portal direction per owner feedback: the post-launch chooser should show `/downloads/android-apk` when the configured APK artifact exists. Removed the public-mode Android gate from `web/portal/dev-server.mjs`, updated `scripts/test-portal-api.sh`, `scripts/verify-launch-staging.mjs`, `scripts/package-launch-staging.sh`, handoff/checklist/docs, and reran `scripts/test-portal-api.sh`; the smoke passed with account/entitlement coverage and restored APK download expectations.
- 2026-06-27: Proved the real game-side starter-card claim path with a normal local QA user. Backed up `server/inc/sqlite/preservation.db` to `tmp/sub-card-claim-qa/preservation-before-20260627T131709Z.db`, created launch-signup account/character `SubQA061818` through the portal into the local game DB (`web_account_id=1`, `starter_card:1=1`), placed the user beside the Lumbridge Void Subscription Vendor, logged in through the desktop workbench client against the running local server, and invoked `/scenario/subscription-vendor-claim`. Proof summary `tmp/sub-card-claim-qa/subscription-vendor-proof-summary.json` shows client inventory item `1602` / `Subscription card`, `starter_card:1` flipped to `2`, one DB inventory card, and one `subscription_vendor` provenance row. Login itself only refreshes subscription state; the card is granted by talking/using the Lumbridge vendor.
- Next gates: deploy/sync the restored APK portal behavior if live should show it, run real iPhone Safari/Home Screen QA using `tmp/iphone-web-qa-live-prelaunch-v1/iphone-safari-qa-report.md`, run physical Android QA before broadly promoting the APK, run hosted deploy verification after the exact target bundle is synced/restarted, and if any later bundle is synced, refill/archive that bundle's generated release handoff with the new backup paths/source-disclosure confirmation.
